var Layout;

Layout = can.Control.extend({
  defaults: {
    inited: false
  }
}, {
  init: function(element, options) {
    var self;
    self = this;
    return console.log('initing layout');
  }
});
