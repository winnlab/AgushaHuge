import Controller from 'controller'

export default Controller.extend(
	{
        defaults: {
            viewpath: 'js/app/user/modules/main/views/'
        }
    }, {
		after_init: function(data) {
			
		}
    }
);